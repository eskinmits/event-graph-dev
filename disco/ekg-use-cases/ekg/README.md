Vendored unchanged from the ekg-explore Disco app (Mustafa Eskin), pulled 2026-09-24:
`js/core.js`, `js/graphview.js` and `css/app.css` (as `ekg.css`). They give ekg-use-cases the
same design system, node vocabulary and graph component as ekg-explore. Do not edit here;
re-copy from a fresh `disco pull ekg-explore` when the explorer's design changes.
